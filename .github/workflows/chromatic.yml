# .github/workflows/chromatic.yml

# Workflow name
name: 'Chromatic'

# Event for the workflow
on: push

# List of jobs
jobs:
  chromatic-deployment:
    # Operating System
    runs-on: ubuntu-latest
    strategy:
      matrix:
        project: [supernova, galaxy, starburst, milky_way, pluto]
        include:
          - project: supernova
            buildScriptName: build-storybook-supernova
            chromaticSecretKey: { { secrets.CHROMATIC_SUPERNOVA } }
          - project: galaxy
            buildScriptName: build-storybook-galaxy
            projectToken: ${{ secrets.CHROMATIC_GALAXY}}
          - project: starburst
            buildScriptName: build-storybook-starburst
            projectToken: ${{ secrets.CHROMATIC_STARBURST}}
          - project: milky_way
            buildScriptName: build-storybook-milky-way
            projectToken: ${{ secrets.CHROMATIC_MILKY_WAY}}
          - project: pluto
            buildScriptName: build-storybook-pluto
            projectToken: e050e0235415
    # Job steps
    steps:
      - uses: actions/checkout@v1
      - name: Install dependencies
        run: yarn
        # 👇 Adds Chromatic as a step in the workflow
      - name: Publish to Chromatic
        uses: chromaui/action@v1
        # Chromatic GitHub Action options
        with:
          # 👇 Chromatic projectToken, refer to the manage page to obtain it.
          buildScriptName: ${{ matrix.buildScriptName }}
          projectToken: ${{ matrix.chromaticSecretKey}}
          onlyChanged: true
          debug: true
